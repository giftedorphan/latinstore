<%= form_for(categorize) do |f| %>
  <% if categorize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categorize.errors.count, "error") %> prohibited this categorize from being saved:</h2>

      <ul>
      <% categorize.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
